{
  "name": "pi-manager",
  "version": "0.1.0",
  "private": true,
  "description": "Local control plane for Pi: providers, models, and live import into ~/.pi/agent.",
  "type": "module",
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node --watch src/server.mjs",
    "dev:web": "npm --prefix web run dev",
    "build:web": "npm --prefix web run build",
    "test": "node --test",
    "check": "node --check src/server.mjs && node --check src/store.mjs && node --check src/profile.mjs && node --check src/gateway.mjs && node --check src/pi-auth.mjs && node --check src/pi-import.mjs && node --check src/provider-discovery.mjs && node --check src/pi-apply.mjs && node --check src/pi-native.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
